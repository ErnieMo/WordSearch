{
    "name": "wem/wordsearch",
    "type": "project",
    "require": {
        "php": ">=8.2",
        "vlucas/phpdotenv": "^5.6"
    },
    "autoload": {
        "psr-4": {
            "App\\": "app/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "@php -r \"if(!is_dir('storage')) mkdir('storage', 0775, true);\""
        ],
        "post-update-cmd": []
    }
}